// alerts/vaultsTracker.jsimport "dotenv/config";import { sendTelegramMessage } from "../telegram.js";import { VAULTS } from "../vaultsConfig.js";const chatId = process.env.CHAT_ID;const ETHERSCAN_API_URL =  process.env.ETHERSCAN_API_URL || "https://api.etherscan.io/v2/api";const ETHERSCAN_API_KEY = process.env.ETHERSCAN_API_KEY;const CHAIN_ID = process.env.CHAIN_ID || "1";const CHECK_INTERVAL_SECONDS = parseInt(  process.env.CHECK_INTERVAL_SECONDS || "30",  10);const CHECK_EVERY_MS = CHECK_INTERVAL_SECONDS * 1000;// petit throttle pour éviter les rate limits Etherscanconst ETHERSCAN_DELAY_MS = parseInt(  process.env.ETHERSCAN_DELAY_MS || "600",  10);// ⏱ timestamp de démarrage du process (pour ne jamais envoyer des tx "d'avant")const BOT_START_TS = Date.now();// pointeurs en mémoire: key = vaultAddress -> "block:txIndex"const pointers = new Map();// anti-doublons: key = vaultAddress -> Set(hash)const seenHashes = new Map();// -------- Helpers --------// Adresse utilisée pour scanner les transferts de tokens// (controller si défini, sinon vault)function getTokenScanAddress(vault) {  return vault.controllerAddress || vault.vaultAddress;}function parseRows(raw) {  try {    if (!raw) return [];    if (Array.isArray(raw)) return raw;    if (Array.isArray(raw.result)) return raw.result;    if (raw && typeof raw === "object" && Array.isArray(raw.result)) {      return raw.result;    }  } catch (e) {    // ignore  }  return [];}// Parse "NNNN:LL" → { have, block, index }function parsePointer(ptr) {  if (typeof ptr !== "string") {    return { have: false, block: 0, index: -1, raw: ptr, reason: "not_string" };  }  const m = /^(\d+):(\d+)$/.exec(ptr.trim());  if (!m) {    return { have: false, block: 0, index: -1, raw: ptr, reason: "bad_format" };  }  return {    have: true,    block: parseInt(m[1], 10),    index: parseInt(m[2], 10),    raw: ptr  };}function trimZeros(s) {  if (s.indexOf(".") === -1) return s;  s = s.replace(/0+$/, "");  if (s.endsWith(".")) s = s.slice(0, -1);  return s;}function sleep(ms) {  return new Promise((resolve) => setTimeout(resolve, ms));}// -------- Etherscan: tokentx only --------// Récupère les transferts ERC20 pour tokenScanAddress + tokenAddress,// et regroupe par hash: on obtient un "event" par transaction.async function fetchTokenEvents(vault) {  if (!ETHERSCAN_API_KEY) {    throw new Error("ETHERSCAN_API_KEY missing in .env");  }  const TOKEN_SCAN_ADDR = getTokenScanAddress(vault);  const TOKEN_SCAN_ADDR_LC = TOKEN_SCAN_ADDR.toLowerCase();  // 🔑 multi-chain : on prend chainId sur le vault, sinon fallback env  const chainId = vault.chainId || CHAIN_ID;  const url =    `${ETHERSCAN_API_URL}?chainid=${chainId}` +    `&module=account&action=tokentx` +    `&address=${TOKEN_SCAN_ADDR}` +    `&contractaddress=${vault.tokenAddress}` +    `&page=1&offset=100&sort=desc` +    `&apikey=${ETHERSCAN_API_KEY}`;  // throttle simple pour limiter les erreurs "Max rate limit"  await sleep(ETHERSCAN_DELAY_MS);  const res = await fetch(url);  if (!res.ok) {    console.error(      `❌ Etherscan HTTP error (tokentx) for ${vault.name}:`,      res.status,      await res.text()    );    return [];  }  const json = await res.json();  // Etherscan v2: status: "1" = OK, "0" = NOTOK  if (!json || json.status !== "1" || !Array.isArray(json.result)) {    console.error(      `⚠️ Etherscan response NOTOK (tokentx) for ${vault.name}:`,      json    );    return [];  }  const rows = parseRows(json);  const TOKEN_ADDR_LC = (vault.tokenAddress || "").toLowerCase();  const defaultDec = vault.tokenDecimals ?? 18;  const defaultSym = vault.tokenSymbol || "TOKEN";  // hash → event agrégé  const map = new Map();  for (const tx of rows) {    const ca = (tx.contractAddress || "").toLowerCase();    if (ca !== TOKEN_ADDR_LC) continue;    const hash = tx.hash || "";    const hashLc = hash.toLowerCase();    if (!hashLc) continue;    const blockNumber = parseInt(tx.blockNumber, 10) || 0;    const txIndex = parseInt(tx.transactionIndex, 10) || 0;    const tsMs = Number(tx.timeStamp || "0") * 1000;    const fn = (tx.functionName || "").toLowerCase();    const dec = parseInt(tx.tokenDecimal || defaultDec, 10) || defaultDec;    const sym = tx.tokenSymbol || defaultSym;    const valueRaw = BigInt(tx.value || "0");    const fromLc = (tx.from || "").toLowerCase();    const toLc = (tx.to || "").toLowerCase();    let ev = map.get(hashLc);    if (!ev) {      ev = {        hash,        blockNumber,        txIndex,        timeMs: tsMs,        functionName: fn,        in: 0n,        out: 0n,        dec,        sym,        depositSender: null,        withdrawRecipient: null      };    } else {      // même tx: on garde les max pour sécurité      if (blockNumber > ev.blockNumber) ev.blockNumber = blockNumber;      if (txIndex > ev.txIndex) ev.txIndex = txIndex;      if (tsMs > ev.timeMs) ev.timeMs = tsMs;      if (fn && !ev.functionName) ev.functionName = fn;    }    // flux entrant vers tokenScanAddress    if (toLc === TOKEN_SCAN_ADDR_LC) {      ev.in += valueRaw;      if (!ev.depositSender) ev.depositSender = fromLc;    }    // flux sortant depuis tokenScanAddress    if (fromLc === TOKEN_SCAN_ADDR_LC) {      ev.out += valueRaw;      if (!ev.withdrawRecipient) ev.withdrawRecipient = toLc;    }    ev.dec = dec;    ev.sym = sym;    map.set(hashLc, ev);  }  // Array d'events agrégés  return Array.from(map.values());}// -------- Traitement d'un vault --------async function processVault(vault) {  const VAULT_ADDR_LC = vault.vaultAddress.toLowerCase();  const TOKEN_SCAN_ADDR = getTokenScanAddress(vault);  const MIN_AMOUNT = vault.minAmount || 0;  const events = await fetchTokenEvents(vault);  const key = VAULT_ADDR_LC;  const rawPtr = pointers.get(key) || "";  const p = parsePointer(rawPtr || "");  let seen = seenHashes.get(key);  if (!seen) {    seen = new Set();    seenHashes.set(key, seen);  }  const out = [];  for (const ev of events) {    const { hash, blockNumber, txIndex, timeMs, functionName } = ev;    const hashLc = (hash || "").toLowerCase();    if (!hashLc) continue;    // anti-doublon    if (seen.has(hashLc)) continue;    // ne pas renvoyer ce qui est antérieur aux premières secondes du bot    if (timeMs && timeMs < BOT_START_TS) continue;    // filtre par pointeur (block:txIndex) si on en a déjà un    if (p.have) {      if (blockNumber < p.block) continue;      if (blockNumber === p.block && txIndex <= p.index) continue;    }    const dec = ev.dec ?? (vault.tokenDecimals ?? 18);    const sym = ev.sym || vault.tokenSymbol || "TOKEN";    // déterminer Deposit / Withdraw    let type = "";    const fn = (functionName || "").toLowerCase();    if (fn.includes("deposit")) type = "Deposit";    else if (fn.includes("withdraw") || fn.includes("redeem")) type = "Withdraw";    else {      // fallback: direction du flux      if (ev.in > 0n && ev.out === 0n) type = "Deposit";      else if (ev.out > 0n && ev.in === 0n) type = "Withdraw";    }    if (!type) continue;    // Montant & from/to "token"    let rawAmount = 0n;    let from = "";    let to = "";    if (type === "Deposit") {      rawAmount = ev.in;      from = ev.depositSender || "";      to = TOKEN_SCAN_ADDR.toLowerCase();    } else {      rawAmount = ev.out;      from = TOKEN_SCAN_ADDR.toLowerCase();      to = ev.withdrawRecipient || "";    }    if (rawAmount <= 0n) continue;    // -------- Filtre whitelist par vault --------    const whitelist = Array.isArray(vault.whitelistedAddresses)      ? vault.whitelistedAddresses.map((addr) => addr.toLowerCase())      : [];    if (whitelist.length > 0) {      const fromLc = (from || "").toLowerCase();      const toLc = (to || "").toLowerCase();      const isWhitelisted =        (fromLc && whitelist.includes(fromLc)) ||        (toLc && whitelist.includes(toLc));      if (isWhitelisted) {        console.log(          `[${vault.name}] tx ignorée (adresse whitelistée)`,          { hash, from, to }        );        // On marque quand même le hash comme vu pour éviter de le revoir        seen.add(hashLc);        continue;      }    }    const amountNum = Number(rawAmount) / Math.pow(10, dec);    if (amountNum < MIN_AMOUNT) continue;    const whenIso = new Date(timeMs || Date.now()).toISOString();    const amountStr = trimZeros(      amountNum.toFixed(Math.min(dec, 8))    );    out.push({      block: blockNumber,      index: txIndex,      type,      symbol: sym,      amount: amountStr,      from,      to,      hash,      time: whenIso    });    seen.add(hashLc);  }  // envoi dans l'ordre chronologique (ancien -> récent)  out.sort((a, b) => {    if (a.block !== b.block) return a.block - b.block;    return a.index - b.index;  });  for (const it of out) {    const emoji = it.type === "Deposit" ? "🟢" : "🔴";    const msg =      `<b>${emoji} ${it.type} ${it.symbol}</b> on <b>${vault.name}</b>\n` +      `Amount: <b>${it.amount} ${it.symbol}</b>\n` +      (it.from ? `From: <code>${it.from}</code>\n` : "") +      (it.to ? `To: <code>${it.to}</code>\n` : "") +      `Date: ${it.time}\n` +      `Tx: https://etherscan.io/tx/${it.hash}`;    await sendTelegramMessage(chatId, msg);  }  // Mise à jour du pointeur sur la base des events tokentx (API renvoie déjà trié desc)  let newPointer = rawPtr || "";  if (events.length) {    // events vient de Etherscan avec sort=desc -> events[0] est le plus récent    const top = events[0];    newPointer = `${top.blockNumber || 0}:${top.txIndex || 0}`;  }  pointers.set(key, newPointer);  console.log(    "🔎 Debug vault:",    JSON.stringify(      {        vault: vault.name,        vaultAddress: vault.vaultAddress,        tokenScanAddress: TOKEN_SCAN_ADDR,        pointerIn: rawPtr,        pointerValid: p.have,        newPointer,        newItems: out.length      },      null,      2    )  );}// -------- Boucle principale --------async function tickAllVaults() {  for (const vault of VAULTS) {    try {      await processVault(vault);    } catch (err) {      console.error(`❌ Error processing vault ${vault.name}:`, err);    }  }}export function startVaultsTracker() {  console.log(    `🚀 gami-vaults-tracker-bot started (tokentx only, baseline = "no history before start"). Check every ${CHECK_INTERVAL_SECONDS}s.`  );  // 1er passage: si des tx ont eu lieu après le démarrage, elles partent;  // celles d'avant sont filtrées par BOT_START_TS.  tickAllVaults();  setInterval(tickAllVaults, CHECK_EVERY_MS);}